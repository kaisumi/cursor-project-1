<div class="container mx-auto px-4 py-8">
  <div class="max-w-3xl mx-auto bg-white rounded-lg shadow-lg overflow-hidden">
    <div class="p-6">
      <div class="flex items-center mb-6">
        <div class="flex-shrink-0">
          <%= image_tag(@post.user.avatar_url || asset_path('default_avatar.png'), class: 'h-12 w-12 rounded-full') %>
        </div>
        <div class="ml-4">
          <p class="text-lg font-medium text-gray-900">
            <%= @post.user.name %>
          </p>
          <p class="text-sm text-gray-500">
            <%= l @post.created_at, format: :long %>
          </p>
        </div>
      </div>

      <h1 class="text-3xl font-bold mb-4"><%= @post.title %></h1>
      
      <div class="prose max-w-none mb-8">
        <%= simple_format @post.content %>
      </div>

      <div class="flex items-center justify-between border-t pt-4">
        <div class="flex space-x-6">
          <div class="flex items-center space-x-2">
            <i class="far fa-comment text-gray-500"></i>
            <span class="text-gray-600"><%= @post.comments.count %></span>
          </div>
          <div class="flex items-center space-x-2">
            <i class="far fa-heart text-gray-500"></i>
            <span class="text-gray-600"><%= @post.likes.count %></span>
          </div>
        </div>

        <% if @post.editable_by?(current_user) %>
          <div class="flex space-x-4">
            <%= link_to '編集', edit_post_path(@post), class: 'text-blue-600 hover:text-blue-800 font-medium' %>
            <%= button_to '削除', post_path(@post), method: :delete, class: 'text-red-600 hover:text-red-800 font-medium', data: { controller: 'confirmation', action: 'confirmation#confirm' } %>
          </div>
        <% end %>
      </div>
    </div>

    <div class="bg-gray-50 px-6 py-4">
      <h2 class="text-xl font-semibold mb-4">コメント</h2>
      
      <% if user_signed_in? %>
        <%= form_with(model: [@post, Comment.new], local: true, class: 'mb-6') do |f| %>
          <div class="mb-4">
            <%= f.text_area :content, rows: 3, class: 'w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500', placeholder: 'コメントを入力...' %>
          </div>
          <%= f.submit 'コメントする', class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded' %>
        <% end %>
      <% end %>

      <div class="space-y-4">
        <% @post.comments.includes(:user).each do |comment| %>
          <div class="flex space-x-4">
            <div class="flex-shrink-0">
              <%= image_tag(comment.user.avatar_url || asset_path('default_avatar.png'), class: 'h-10 w-10 rounded-full') %>
            </div>
            <div class="flex-grow">
              <div class="bg-white rounded-lg p-4 shadow-sm">
                <div class="flex items-center justify-between mb-2">
                  <span class="font-medium text-gray-900"><%= comment.user.name %></span>
                  <span class="text-sm text-gray-500"><%= time_ago_in_words(comment.created_at) %>前</span>
                </div>
                <p class="text-gray-700"><%= comment.content %></p>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <div data-controller="confirmation" class="hidden">
      <div class="fixed inset-0 bg-gray-500 bg-opacity-75 transition-opacity" data-confirmation-target="overlay"></div>
      <div class="fixed inset-0 z-10 overflow-y-auto">
        <div class="flex min-h-full items-end justify-center p-4 text-center sm:items-center sm:p-0">
          <div class="relative transform overflow-hidden rounded-lg bg-white px-4 pb-4 pt-5 text-left shadow-xl transition-all sm:my-8 sm:w-full sm:max-w-lg sm:p-6">
            <div class="sm:flex sm:items-start">
              <div class="mx-auto flex h-12 w-12 flex-shrink-0 items-center justify-center rounded-full bg-red-100 sm:mx-0 sm:h-10 sm:w-10">
                <svg class="h-6 w-6 text-red-600" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126zM12 15.75h.007v.008H12v-.008z" />
                </svg>
              </div>
              <div class="mt-3 text-center sm:ml-4 sm:mt-0 sm:text-left">
                <h3 class="text-base font-semibold leading-6 text-gray-900">投稿の削除</h3>
                <div class="mt-2">
                  <p class="text-sm text-gray-500">この投稿を削除してもよろしいですか？この操作は取り消せません。</p>
                </div>
              </div>
            </div>
            <div class="mt-5 sm:mt-4 sm:flex sm:flex-row-reverse">
              <button type="button" class="inline-flex w-full justify-center rounded-md bg-red-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-red-500 sm:ml-3 sm:w-auto" data-confirmation-target="confirmButton">削除する</button>
              <button type="button" class="mt-3 inline-flex w-full justify-center rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50 sm:mt-0 sm:w-auto" data-confirmation-target="cancelButton">キャンセル</button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div> 