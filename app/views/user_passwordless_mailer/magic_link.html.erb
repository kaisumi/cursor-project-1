<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
    </style>
  </head>

  <body>
    <p><%= @user.email %> 様</p>
    <p>ログインするには以下のリンクをクリックしてください。</p>
    <p>
      <a href="<%= users_passwordless_session_url(token: @token) %>">ログイン</a>
    </p>
    <p>このリンクは30分間有効です。</p>
  </body>
</html> 